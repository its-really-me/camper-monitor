{
  "name": "camper-monitor",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/reader-battery",
    "packages/reader-solar",
    "packages/reader-starter",
    "packages/ui-fb"
  ],
  "scripts": {
    "start": "node packages/server/src/index.js",
    "dev": "concurrently -n server,ui -c cyan,magenta \"node packages/server/src/index.js\" \"npm run dev --prefix packages/ui\"",
    "build:ui": "npm install --prefix packages/ui && npm run build --prefix packages/ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
